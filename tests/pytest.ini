[pytest]
testpaths = .
python_files = test_*.py
# Avoid recursing into parent folder; use patterns instead of '..' which breaks parsing
norecursedirs = .* build dist venv .venv __pycache__
markers =
	unit: unit tests
	integration: integration tests
	slow: slow tests
